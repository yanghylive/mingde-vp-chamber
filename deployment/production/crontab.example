# ============================================================
# 明德恒智AI企商汇 生产定时任务（crontab）
# 部署：crontab -e 添加下列行（替换路径为生产实际路径）
# 生产 CRMEB 根：/opt/mingde-vp-chamber/backend/crmeb/crmeb
# ============================================================

# 日志目录（先创建：mkdir -p /opt/mingde-vp-chamber/logs）
# 活动报名超时释放 + 订单上下文修复：每 5 分钟跑一次
# （并发场景下多个实例同时跑也安全：任务内部有行锁/幂等/失败重试）
*/5 * * * * cd /opt/mingde-vp-chamber/backend/crmeb/crmeb && /usr/bin/php app/chamber/jobs/cli/repair.php all 50 >> /opt/mingde-vp-chamber/logs/repair.log 2>&1

# 若 CRMEB 队列（Redis）已在生产运行（php think queue:work），
# 本 crontab 仍建议保留 —— repair 任务是修复性兜底，不依赖队列 worker。
